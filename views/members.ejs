<!DOCTYPE html>
<html lang="en">
  <%- include('partials/head', {title: title}) %>
  
  <body>
    <h1> <%= title %> </h1>

    <ul>
      <% for(i=0; i < members.length; i++) {%>
        <li><%= members[i].username %></li>
      <% } %>
    </ul>
  </body>
</html>